apiVersion: v1
kind: Service
metadata:
    name: asistenciaplusemailtestingazure
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: asistenciaplusemailtestingazure